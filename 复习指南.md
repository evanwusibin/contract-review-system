# 合同评审系统 · 全链路项目复习

> **一句话定位**：AI 驱动的合同风险识别与审批工作台，基于原文证据、规则命中与人工会签，帮助法务和业务更快发现合同风险。
> **技术栈**：FastAPI + SQLAlchemy + PostgreSQL + MinIO + RapidOCR + Vite 6
> **仓库**：`evanwusibin/contract-review-system`

---

## 一、项目定位与核心价值

传统合同评审依赖人工逐条比对，效率低、遗漏多、无法追溯。本系统将合同评审拆解为**导入 → 解析 → 规则 → 会签 → 版本**的可追溯流程，输出带证据的风险建议，不替代最终审批。

**面试话术**：
> "我们把合同评审拆成了 7 个可追溯的步骤——导入、质量诊断、OCR 解析、规则命中、人工会签、版本保存、审计留痕。系统只输出建议，最终审批权始终在责任人。"

---

## 二、系统架构（六层）

```
┌─────────────────────────────────────────┐
│           前端（Vite 6 + 原生 JS）        │
│  轮播登录 → 欢迎页 → 指挥中心/待审任务     │
│  深度工作台（双栏） → 版本/规则/审计/治理   │
├─────────────────────────────────────────┤
│           API 层（FastAPI）               │
│  Auth（cookie 会话 + PBKDF2）             │
│  Tasks / Reviews / Imports / Rules/Audit │
├─────────────────────────────────────────┤
│           领域层                          │
│  ContractImporter → ReviewWorkflow       │
│  RestrictedRuleEngine（确定性规则）        │
│  ReviewVersionService（版本快照）          │
├─────────────────────────────────────────┤
│           引擎层                          │
│  ContractParser（RapidOCR）              │
│  QualityDiagnostic（质量诊断）            │
│  ReviewResultService（结论生成）          │
├─────────────────────────────────────────┤
│           基础设施层                      │
│  PostgresReviewStore（持久化）            │
│  ObjectStorage（MinIO 附件）              │
│  AuditService（审计日志）                 │
└─────────────────────────────────────────┘
```

---

## 三、核心实体与状态机

### 领域模型

| 实体 | 说明 |
|------|------|
| Contract（合同） | 待审核的售前或售后文件 |
| ContractVersion（版本） | 新合同、修订或补充协议，历史不可覆盖 |
| ReviewTask（审查任务） | 围绕一个版本执行解析、规则、会签的工作单元 |
| RiskItem（风险项） | 规则或解析质量产生的可定位问题 |
| ReviewConclusion（审批建议） | 系统输出的通过/驳回/复核，≠最终审批 |
| RuleHit（规则命中） | 规则执行结果 + 证据片段 + 来源定位 |
| AuditLog（审计日志） | 每一步操作的完整记录 |

### 状态机

```
imported → parsing → reviewing → awaiting_confirmation → confirmed
                         ├→ blocked
                         └→ rejected_recommendation
```

**关键区分**：`blocked` ≠ `rejected`，`rejected_recommendation` ≠ 最终审批驳回。

---

## 四、核心 API（18 个端点）

### Auth

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/v1/auth/login` | 密码哈希校验，Set-Cookie（HttpOnly） |
| POST | `/v1/auth/logout` | 清除会话 |
| GET | `/v1/auth/me` | 返回当前用户（demo 模式返回匿名 admin） |

### Tasks & Reviews

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/tasks` | 任务列表 |
| GET | `/v1/tasks/{id}` | 任务详情 + 附件 |
| GET | `/v1/tasks/{id}/review` | 完整评审数据（字段/风险/会签/版本） |
| GET | `/v1/tasks/{id}/versions` | 版本历史 |
| GET | `/v1/tasks/{id}/audit` | 审计日志 |
| POST | `/v1/reviews/run` | **核心**：导入+解析+规则+会签+保存，需三方 `confirmations` |
| POST | `/v1/imports` | 单纯导入（不触发规则） |

### Insights（洞察，需新版后端）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/tasks/{id}/flow` | 审批流图（pending → done） |
| GET | `/v1/tasks/{id}/risk-matrix` | 风险矩阵（high/medium/low） |
| GET | `/v1/tasks/{id}/evidence-graph` | 证据图谱（字段↔证据↔规则） |

### Rules / Audit

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/rules` | 规则列表 |
| POST | `/v1/rules` | 新增规则（admin） |
| PATCH | `/v1/rules/{id}` | 发布/退役 |
| GET | `/v1/audit/events` | 全局审计 |

---

## 五、Docker 部署

### 生产栈（`docker-compose.prod.yml`）

```bash
cd contract-review
cp .env.production.example .env.production
# 编辑填写：POSTGRES_PASSWORD / MINIO_SECRET_KEY / SESSION_SECRET / ADMIN_PASSWORD
# CORS_ORIGINS: http://127.0.0.1:5173,http://127.0.0.1:5174
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
```

**服务**：`postgres:16-alpine`（元数据）+ `minio/minio`（附件）+ `contract-review-backend`（RapidOCR，CPU）

### 本地开发栈（`docker-compose.demo.yml`）

```bash
docker compose -f docker-compose.demo.yml up -d  # 零配置，MinIO + 后端
```

---

## 六、前端关键点

### 页面流

```
登录页（轮播 + 表单）→ 欢迎页（静态原型复刻）→ 指挥中心/待审任务
→ 深度工作台（双栏：左证据/右审核 + 底部洞察）→ 版本/规则/审计
```

### 登录轮播（`public/images/`）

- `1.mp4` 视频（5s）→ `2.webp/3-8.png` 图片自动循环
- 3.5s/张，视频结束后切图
- 文案：AI 智能审查 + 4 步流程（导入/解析/风险/会签）

### 深度工作台

- `h-screen overflow-hidden` 视口固定 + `content overflow-y-auto min-h-0` 内部滚动
- 空任务守卫：`taskId` 为空/`null` → "尚未选择评审任务"
- 按钮：返回列表/查看审计/补证/驳回/查看全部 均有事件绑定

### 视觉基准

- 背景：`linear-gradient(135deg, #eef4ff 0%, #f4f1ff 42%, #effaf6 100%)`
- 侧边栏：`220px`，品牌区域 `56px`（与顶栏对齐）
- 品牌：`合同智能评审` 渐变色 + `AI` 椭圆渐变徽标

---

## 七、已知限制

1. **旧 Docker 镜像**（`995411dc`，402 行）：缺 `approvals/pending`、`flow/risk-matrix/evidence-graph` 路由，`rule_hits.result` 仅 `varchar(16)`（已扩容 `32`）
2. **328B 占位 PDF**：`examples/CTR-2026-*.pdf` 仅一行文本，OCR 后字段全 missing
3. **真实合同超时**：`tests/data/` 的 8.7MB 扫描件 RapidOCR 处理超 2 分钟
4. **规则持久化**：新增规则写内存（旧镜像 `rule_repo is None`），需重打镜像才落 PostgreSQL

---

## 八、面试高频问题

**Q：系统怎么处理"证据不足"的场景？**
A：规则引擎对 `missing` 字段输出 `insufficient_evidence`，不自动驳回，由人工补证。

**Q：会签机制怎么实现？**
A：`required_roles` 定义必签角色（business/legal/warranty），全部 `confirmed_roles` 后才生成正式结论。

**Q：与真实审批系统的关系？**
A：系统只输出建议，不接真实审批。回写标记 `SIMULATED_ONLY`。

**Q：如何保证审计可追溯？**
A：`task_logs`（操作日志）+ `comment_logs`（评论日志）全部落库，每一步带 `request_id`。

---

## 九、项目边界

- 教学/实战项目，不含完整企业生产权限体系、灰度发布和多租户治理
- 本地 `.env`、数据库文件、日志不应提交 GitHub
- `examples/` 中的 328B PDF 是占位文件，不是真实合同
- 审批建议 ≠ 最终审批，系统不替代责任人

---

> **复习时间**：30 分钟通读 + 15 分钟 API 试跑
> **快速启动**：`docker compose -f docker-compose.prod.yml up -d` → `npm run dev` → `admin / fa21bc041474b3ae94d7e043`
